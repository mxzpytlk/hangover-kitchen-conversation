type Query {
  """
  Костыль, который нужен для того, чтобы приложение не падало при первом запуске. Необходимо убрать после того,
  как будут добавлены ещё запросы. При попытке обратиться к нему, будет возвращена ошибка, т.к. запрос не реализован
  """
  crutch: String
}